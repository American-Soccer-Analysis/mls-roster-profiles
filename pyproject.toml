[tool.poetry]
name = "mls-roster-profiles"
version = "0.1.0"
description = ""
authors = [
    "Tyler Richardett <tyler.richardett@gmail.com>",
]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.11,<3.12"
loguru = "^0.7.2"
pydantic = "^2.12.5"
pandas = "^3.0.2"
pypdf = "^6.9.2"
parsimonious = "^0.11.0"
itscalledsoccer = "^1.3.3"
rapidfuzz = "^3.14.3"

[tool.poetry.group.dev.dependencies]
bandit = { version = "1.9.4", extras = ["toml"] }
pre-commit = "4.5.1"
isort = "^8.0.1"
docformatter = "^1.7.5"
ipykernel = "^7.2.0"
ruff = "^0.15.8"

[tool.poetry.group.test.dependencies]
pytest-cov = "7.1.0"
pytest-mock = "<3.15.2"
pytest-runner = "^6.0.0"
pytest = "9.0.2"
pytest-github-actions-annotate-failures = "^0.4.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 120
fix = true
select = [
    # flake8-2020
    "YTT",
    # flake8-bandit
    "S",
    # flake8-bugbear
    "B",
    # flake8-builtins
    "A",
    # flake8-comprehensions
    "C4",
    # flake8-debugger
    "T10",
    # flake8-simplify
    "SIM",
    # isort
    "I",
    # mccabe
    "C90",
    # pycodestyle
    "E",
    "W",
    # pyflakes
    "F",
    # pygrep-hooks
    "PGH",
    # pyupgrade
    "UP",
    # ruff
    "RUF",
    # tryceratops
    "TRY",
]
ignore = [
    # LineTooLong
    "E501",
    # DoNotAssignLambda
    "E731",
]

[tool.ruff.format]
preview = true

[tool.ruff.per-file-ignores]
"tests/*" = ["S101"]

[tool.docformatter]
recursive = true
pre-summary-newline = true
blank = true
in-place = true
black = true
style = "google"
